fastapi==0.115.12
fpdf==1.7.2
httpx==0.28.1
motor==3.7.1
pandas==2.3.0
pydantic==2.11.5
python-dotenv==1.1.0
sqlalchemy==2.0.25
sqlalchemy-utils==0.37.8
uvicorn==0.23.2
httpx==0.28.1
requests==2.32.3
pymongo==4.6.0
app==0.0.1
# Note: The version numbers are examples and may need to be updated based on compatibility and requirements.
# Ensure to pin versions to avoid breaking changes in production.
# Additional dependencies can be added as needed.
# This file is used to manage the Python dependencies for the server-side application.
# It is recommended to use a virtual environment to install these dependencies.
# To install the dependencies, run:
# pip install -r requirements.txt
# This file is part of the server-side application for the project.